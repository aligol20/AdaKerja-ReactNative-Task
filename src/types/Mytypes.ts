/**
 * It's Simple ts file with some enums
 * I used them to reduce volume of codes and pass props easily
 * most of them are usable in several places
 */

export enum RouteState {
  loading = ' loading',
  validation = 'validation',
  logged = 'logged',
  signIn = 'signIn',
}
export enum OverViewType {
  HomePage = 'HomePage',
}
